<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.2.xsd">

    <changeSet author="Danil" id="10">
        <sql>
            INSERT INTO employees(first_name, last_name, bonus_coefficient)
            VALUES('Mister', 'X', 1.5);

            INSERT INTO employees(first_name, last_name, bonus_coefficient)
            VALUES('Mister', 'Y', 1.2);

            INSERT INTO employees(first_name, last_name, bonus_coefficient)
            VALUES('Mister', 'Z', 1.2);
        </sql>
    </changeSet>

    <changeSet author="Danil" id="11">
        <sql>
            INSERT INTO rewards(employee_id, job_type_id, reward_status)
            VALUES (
            (SELECT id FROM employees WHERE first_name = 'Mister' AND last_name = 'X' AND bonus_coefficient = 1.5),
            (SELECT id FROM tariffs WHERE job_type_id = (SELECT id FROM job_types WHERE job_type = 'SPEECH')),
            'UNPAID'
            );

            INSERT INTO rewards(employee_id, job_type_id, reward_status)
            VALUES (
            (SELECT id FROM employees WHERE first_name = 'Mister' AND last_name = 'Y' AND bonus_coefficient = 1.2),
            (SELECT id FROM tariffs WHERE job_type_id = (SELECT id FROM job_types WHERE job_type = 'LESSON')),
            'UNPAID'
            );

            INSERT INTO rewards(employee_id, job_type_id, reward_status)
            VALUES (
            (SELECT id FROM employees WHERE first_name = 'Mister' AND last_name = 'Z' AND bonus_coefficient = 1.2),
            (SELECT id FROM tariffs WHERE job_type_id = (SELECT id FROM job_types WHERE job_type = 'HELP')),
            'UNPAID'
            );
        </sql>
    </changeSet>

</databaseChangeLog>